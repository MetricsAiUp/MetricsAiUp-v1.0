DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=3001
NODE_ENV=development
# 32-byte hex (64 chars). Generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
IMAP_ENCRYPTION_KEY=""
